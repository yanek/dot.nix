{
  comment = {
    enable = true;
    setupOpts = {
      options = {
        ignore_blank_line = true;
        pad_comment_parts = false;
      };
      mappings = {
        comment_visual = "<C-/>";
        textobject = "<C-/>";
      };
    };
  };
  icons = {
    enable = true;
  };
  notify = {
    enable = true;
  };
  pairs = {
    enable = true;
  };
  statusline = {
    enable = true;
  };
  tabline = {
    enable = true;
  };
}
